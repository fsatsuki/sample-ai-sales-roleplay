{
  "name": "cross-region-parameter-handler",
  "version": "1.0.0",
  "description": "Lambda function for cross-region SSM parameter retrieval",
  "main": "cross-region-parameter.js",
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.835.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
